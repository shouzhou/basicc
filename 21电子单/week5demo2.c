#include<stdio.h>
int main()
{
	int a,b,min;
	float f;
	double dd;
	char cc;
	
	printf("size of int = %d float = %d  double = %d char = %d\n",sizeof(a),sizeof(f),sizeof(dd),sizeof(cc));
//	a= 3;
//	b= 4;
	// 逻辑  非零为真  
//	if(1) //肯定满足 
    //while(1)  
//	min = a-3 ? a:b; //a>b?a:b; 
	// 先判断？前面的表达式 是否逻辑成立（真） ，如果正确取：前的值
	// 如果 不成立 ，取 ：后的值 
//	printf("min = %d\n",min); //3 3
	return 0;
} 
